# cmake build with  [cmakepreset.json](./CMakePresets.json)

    cmake  --preset "linux-debug"
    
    cmake --build  --preset "verbose-build-linux"  
    
    cmake --build out/build/linux-debug/ --target install

